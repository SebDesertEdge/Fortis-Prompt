using UnityEngine;

namespace Fortis.Analytics.PlayerLoop
{
    /// <summary>
    /// Empty MonoBehaviour that serves as a scene anchor for the
    /// ResilientAnalyticsInspector custom editor. Drop this component on any
    /// GameObject to get a read-only analytics health panel in the Inspector.
    /// </summary>
    public class AnalyticsMonitor : MonoBehaviour { }
}
